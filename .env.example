# ==========================================
# VECS SERVER CONFIGURATION
# ==========================================

# --- Parametri AI (Cache L2) ---

# Soglia di similarità (0.0 - 1.0) per considerare una risposta "valida".
# 0.65 è ottimo per modelli quantizzati come BGE-M3 o MiniLM.
# Abbassa se ottieni troppi MISS su frasi simili. Alza se ottieni falsi positivi.
VECS_L2_THRESHOLD=0.65

# Soglia di deduplicazione (0.0 - 1.0).
# Se una nuova frase è simile al 95% a una esistente, NON viene salvata per risparmiare spazio.
VECS_L2_DEDUPE_THRESHOLD=0.95

# Numero massimo di vettori da mantenere in RAM.
# Dipende dalla memoria disponibile (es. 5000 vettori * 1024 float * 4 byte ~= 20MB + overhead)
VECS_L2_CAPACITY=10000

# --- Percorso Modello ---

# Opzione A: Usa il modello scaricato automaticamente nel Docker (Default)
VECS_MODEL_PATH=/app/models/default_model.gguf

VECS_TTL_DEFAULT=3600
VECS_SAVE_INTERVAL=60